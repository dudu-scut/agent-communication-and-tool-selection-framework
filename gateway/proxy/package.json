{
  "name": "nexusai-grpc-proxy",
  "version": "1.0.0",
  "description": "JSON ↔ gRPC transcoding proxy for NexusAI (replaces Envoy in dev)",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "typecheck": "node --check server.mjs",
    "lint": "npm run typecheck",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.12.0",
    "@grpc/proto-loader": "^0.7.13"
  }
}
